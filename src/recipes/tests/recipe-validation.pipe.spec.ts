import { RecipeValidationPipe } from '../pipes/recipe-validation.pipe';

describe('RecipeValidationPipe', () => {
  it('should be defined', () => {
    expect(new RecipeValidationPipe()).toBeDefined();
  });
});
