import { RatersValidationPipe } from '../pipes/raters-validation.pipe';

describe('RatersValidationPipe', () => {
  it('should be defined', () => {
    expect(new RatersValidationPipe()).toBeDefined();
  });
});
