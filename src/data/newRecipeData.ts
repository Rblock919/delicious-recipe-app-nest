/* instruction for this file:
    * Only use this file when adding new recipes and data model has not changed for recipes since the last time some were added
    1. Enter new recipe data (delete existing if it has already been added back to recipeData.js)
    2. run the route /admin/AddNewRecipes
    3. cut, paste to append recipe data code into the ./recipeData.js file
*/
const recipedata = [];

export default recipedata;
